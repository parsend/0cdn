# 0cdn client (zero-trust VPN)
0CDN_SERVER_URL=https://your-server
0CDN_TOKEN=your_token_from_dashboard
0CDN_SOCKS_ADDR=127.0.0.1:1080
# optional: prefer QUIC to exits (then TCP fallback)
# 0CDN_USE_QUIC=1
# optional: TUN exit by 5-word node ID or direct addr
# 0CDN_EXIT_NODE_ID=word:word:word:word:word
# 0CDN_TUN_GW_ADDR=exit_ip:port
# optional: register as P2P exit (requires public addr)
# 0CDN_P2P_EXIT=1
# 0CDN_P2P_ICE=1
# 0CDN_P2P_ADDR=1.2.3.4:4433
# 0CDN_P2P_LISTEN=:4433
# 0CDN_STUN_URL=stun:stun.l.google.com:19302
# optional: DHT fallback when server blocked (comma-separated addrs or from GET /api/dht/bootstrap)
# 0CDN_DHT_BOOTSTRAP=1.2.3.4:4000
# 0CDN_DHT_CACHE_TTL=300
# optional: TUN overlay (Linux, CAP_NET_ADMIN), subnet 10.200.0.0/24
# 0CDN_TUN=1
# 0CDN_TUN_IFACE=
# 0CDN_TUN_CIDR=10.200.0.2/24
# 0CDN_TUN_GW=10.200.0.1
# optional: post-quantum and token auth (must match agent)
# 0CDN_PQ=1
# 0CDN_AGENT_AUTH=1
# optional: masking (must match agent)
# 0CDN_MASK_TLS=1
# 0CDN_MASK_PADDING=1
# 0CDN_MASK_PADDING_MAX=64
# 0CDN_MASK_MORPH=1
# optional: Tor/I2P proxy
# 0CDN_TOR_PROXY=socks5://127.0.0.1:9050
# 0CDN_I2P_PROXY=http://127.0.0.1:4444
