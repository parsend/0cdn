# 0cdn server (control plane)
0CDN_DB=./0cdn.db
0CDN_SERVER_ADDR=:8443
# optional: DNS for .0cdn zone
# 0CDN_DNS_ADDR=:53
# optional: max request body size in MB (default 1, max 64)
# 0CDN_MAX_BODY_MB=1
# optional: GET /api/config returns these for clients/agents (P2P ICE)
# 0CDN_STUN_URL=stun:stun.l.google.com:19302
# 0CDN_TURN_URL=
