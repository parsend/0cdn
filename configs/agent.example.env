# 0cdn agent (edge node)
0CDN_SERVER_URL=https://your-server
0CDN_TOKEN=your_token_from_dashboard
0CDN_DATA=.
0CDN_TCP_ADDR=:4433
0CDN_HTTP_ADDR=:8080
# optional: QUIC on 443 (same proto as TCP)
# 0CDN_USE_QUIC=1
# 0CDN_QUIC_ADDR=:443
# optional: TUN overlay (Linux, CAP_NET_ADMIN), subnet 10.200.0.0/24
# 0CDN_TUN=1
# 0CDN_TUN_IFACE=
# 0CDN_TUN_CIDR=10.200.0.1/24
# optional: DHT bootstrap node (others fetch from GET /api/dht/bootstrap)
# 0CDN_DHT_LISTEN=:4000
# optional: masking
# 0CDN_MASK_TLS=1
# 0CDN_MASK_PADDING=1
# 0CDN_MASK_PADDING_MAX=64
# 0CDN_MASK_MORPH=1
# optional: post-quantum and token auth on tunnel (must match client)
# 0CDN_PQ=1
# 0CDN_AGENT_AUTH=1
# 0CDN_TLS_CERT=/path/to/cert.pem
# 0CDN_TLS_KEY=/path/to/key.pem
# optional: Tor/I2P proxy for server requests
# 0CDN_TOR_PROXY=socks5://127.0.0.1:9050
# 0CDN_I2P_PROXY=http://127.0.0.1:4444
